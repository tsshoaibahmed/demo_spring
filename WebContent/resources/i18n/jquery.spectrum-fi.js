// Spectrum Colorpicker
// Finnish (fi) localization
// https://github.com/bgrins/spectrum

(function ( $ ) {

    var localization = $.spectrum.localization["en"] = {
        cancelText: "Cancel",
        chooseText: "Choose"
    };

    $.extend($.fn.spectrum.defaults, localization);

})( jQuery );
